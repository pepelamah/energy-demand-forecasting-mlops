version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app
    command: uvicorn energy_demand_forecasting_mlops.api.main:app --host 0.0.0.0 --port 8000 --reload
